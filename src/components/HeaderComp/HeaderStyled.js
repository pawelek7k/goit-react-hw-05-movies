import styled from "styled-components";

const StyledHeader = styled.header`
  padding: 1rem 3rem;
  border-bottom: 1px solid #47434a;
  ul {
    display: flex;
    gap: 4rem;
  }
`;

export default StyledHeader;
